#!/usr/bin/bash

set -eu

echo "Deleting saved models and summaries from server $1..."
ssh -T -i "~/.ssh/canada-central.pem" $1 <<ENDSSH
set -e
if [ -d "ml-experiments" ]; then
    cd ml-experiments
    rm -rf saved_models saved_summaries
fi
ENDSSH
